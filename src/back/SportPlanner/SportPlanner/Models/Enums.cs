namespace SportPlanner.Models;

public enum Gender
{
    Male = 0,
    Female = 1,
    Mixed = 2
}

public enum TeamLevel
{
    A = 0, // Nivel alto
    B = 1, // Nivel medio
    C = 2  // Nivel básico
}

public enum UserRole
{
    Administrator = 0,
    Director = 1,
    Coach = 2,
    Assistant = 3
}