# SportPlanner src repository

## 📁 Documentación del proyecto

La documentación técnica del proyecto está centralizada en la carpeta:

`C:\Proyectos\SportPlanner\DocSportPlanner`

Esta carpeta incluye especificaciones, modelo de datos, OpenAPI (`DocSportPlanner/docs/openapi.yaml`) y decisiones arquitectónicas (ADR). Antes de iniciar cualquier tarea (both `back` and `front`), revisa estos documentos para alinear requisitos, contratos de API y convenciones de diseño.

> 💡 Tip: Las `instructions` y `agents` que se creen para issues o PRs deben comprobar la `DocSportPlanner` como la fuente de la verdad antes de comenzar el trabajo.

